<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Camera Hardware Feature -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.TripAndTicketOCR">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Broadcast Receiver for Alarms -->
        <receiver
            android:name=".notification.ParkingAlarmReceiver"
            android:exported="false" />

        <!-- Broadcast Receiver for Parking Expiry (fires at exact expiry time) -->
        <receiver
            android:name=".notification.ParkingExpiredReceiver"
            android:exported="false" />

        <!-- Broadcast Receiver for Notification Action Buttons (Extend / Cancel) -->
        <receiver
            android:name=".notification.ParkingExtendReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mndublo.odolens.ACTION_EXTEND_1H" />
                <action android:name="com.mndublo.odolens.ACTION_CANCEL_PARKING" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
